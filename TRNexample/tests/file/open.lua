-- It would be impactful if we could actually see useful information about the file like it's name/position
-- Instead of what we do see - a strange hex pointer.
function main(Data)
   local AFile = io.open("tests/file/open.lua");
   AFi😊
end