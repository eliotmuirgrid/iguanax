### This file doesn't do anything. It's just a list of the flags we would use in a configure script to generate the headers we have hand crafted.

--disable-shared --enable-static --disable-pcre16 --disable-pcre32 --enable-pcre8 --disable-cpp --enable-utf --enable-valgrind --disable-stack-for-recursion 
