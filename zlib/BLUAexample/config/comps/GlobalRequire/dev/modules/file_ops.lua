local Config = require("modules.config")
local Utils = require("modules.utils")
local FileOps = {}

function FileOps.getPathSeparator()
   return 
end

function FileOps.ensureDirectoryExists(dirPath)
   return
end

function FileOps.directoryExists(dirPath)
   return
end

function FileOps.removeDirectory(dirPath)
  return
end

function FileOps.deleteFolder(folderPath)
   return
end

function FileOps.copyDirectory(sourcePath, destPath)
   return
end

function FileOps.copyCompFolder(sourcePath, destiPath)
   return
end

function FileOps.removeAllGitDirs(folderPath)
   return
end

function FileOps.readFile(fileName)
  return
end

function FileOps.writeFile(fileName, content)
  return
end

function FileOps.copyAllComponentFolders(sourceCompsPath, destCompsPath, compTags)
   return
end

return FileOps

