// this dummy source file is needed to allow us to 
// set the "exclude from build" flag on the .cpp files (except for this one),
// and thus enable or disable this LUA implementation per configuration
// e.g. x86 uses JIT and x64 uses non-JIT
// --Dmitri

void LUAdummyFunctionToEnsureLibraryCreation514()
{
};
